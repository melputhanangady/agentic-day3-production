# agentic-day3-production

# Goto Folder which you have script

```
conda create --prefix ./env python=3.12 -y
conda activate ./env 
pip install -r requirements.txt

python app.py```

WARNING: Ensure that you do not commit .env files, please add .env file to .gitignore

RUN the app using the command 

```
python app.py

```